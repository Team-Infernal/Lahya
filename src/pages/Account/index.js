import Admin from "../../components/Account/Admin";
import config from "../../config.js";

const Account = () =>{
    return(
        <div id= "Account">
            <Admin />
        </div>
    )
}

export default Account;
